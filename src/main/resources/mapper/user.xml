<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dflong.greenbull.mapper.UserMapper">

    <select id="getUser" resultType="com.dflong.greenbull.entity.User">
        SELECT * FROM ${tableName} where id = #{id} and unionid = #{unionid} limit 1
    </select>

    <update id="updateUser" >
        update user set nickname = #{nickname} where id = #{id}
    </update>
</mapper>